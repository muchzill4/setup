alias ll='ls -l'
alias la='ls -a'
alias lla='ls -la'
alias c='clear'
alias md='mkdir'
alias z='fasd_cd -d'
alias t='tmux attach || tmux -u new'

# vagrant
alias vu='vagrant up'
alias vh='vagrant halt'
alias vr='vagrant reload'
alias vp='vagrant provision'
alias vs='vagrant ssh'

# vim
alias v='vim'
alias m='mvim'

# gspiu
alias gspiu='git pull && git submodule init && git submodule update && git submodule status'
